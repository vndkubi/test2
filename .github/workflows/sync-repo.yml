# 📄 .github/workflows/sync-upstream.yml
name: Sync with upstream and always create PR

on:
  schedule:
    - cron: '0 * * * *' # ⏰ Mỗi giờ
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout forked repo
        uses: actions/checkout@v4

      - name: Setup Git config
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Add upstream remote
        run: git remote add upstream https://github.com/ctson97/test1.git

      - name: Fetch and create sync branch
        run: |
          git fetch upstream
          BRANCH=sync-upstream-$(date +%s)
          git checkout -b $BRANCH upstream/main
          git push origin $BRANCH
          echo "BRANCH_NAME=$BRANCH" >> $GITHUB_ENV

      - name: Install GitHub CLI
        uses: cli/cli-action@v2

      - name: Create PR even if no diff
        run: |
          gh pr create \
            --title "🔄 Sync upstream even with no changes" \
            --body "Đồng bộ hóa với repo gốc (có thể không có thay đổi)." \
            --head ${{ env.BRANCH_NAME }} \
            --base main
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
